class Quations {
  late String questionText;
  late bool questionAnswer;
  Quations(this.questionText, this.questionAnswer);
  // Quations(String q, bool a) {
  //   questionText = q;
  //   questionAnswer = a;
  // }
  // late String questionText;
  // late bool questionAnswer;
  //
  // Quations({String q, required bool a}) {
  //   questionText = q;
  //   questionAnswer = a;
  // }
}
